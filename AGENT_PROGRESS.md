# AGENT_PROGRESS.md � Memori Agent Lintas Sesi

> **WAJIB DIBACA** di awal setiap sesi baru sebelum melanjutkan pekerjaan.
> Jika ada perbedaan keputusan teknis antara file ini dan dokumen rencana awal, **ikuti file ini** karena ini representasi keputusan final yang sudah dieksekusi di kode.

| Fase | Tanggal    | Ringkasan                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 | Keputusan Teknis                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       | Pending (Keputusan untuk phase selanjutnya)                                                                                                                                                                                                                                      |
| ---- | ---------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 0    | 2026-07-13 | Bootstrap monorepo selesai. Turborepo 2.x + pnpm 11.9.0 workspaces. Struktur folder sesuai �19 PRD persis. Prisma 6.19.3 init di `packages/db` dengan model placeholder `HealthCheck` � `prisma generate` berhasil. Docker Compose (PostgreSQL 18 + Redis 8) dengan health checks. CI skeleton GitHub Actions (install ? prisma generate ? lint ? build). NestJS v11 skeleton: `services/api` (port 3000), `services/notification` (BullMQ + port 3001), `services/ai-gateway` (port 3002) � semua dengan `/health` endpoint. `services/inventory` = README placeholder saja (�18.1). Next.js 16.2.10 skeleton: `apps/web` (port 4000), `apps/admin` (port 4001). Semua packages (`db`, `types`, `ui`, `auth`, `ai`) = placeholder exports. ESLint + Prettier dikonfigurasi di root monorepo. Git repository di-inisialisasi dan commit pertama dilakukan. `pnpm build` 10/10 sukses, `pnpm lint` 11/11 sukses. Tidak ada secret hardcode.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | NestJS v11, Next.js 16.2.10, Node 24 Alpine (Docker), PostgreSQL 18, Redis 8, Prisma 6.19.3, Turborepo 2.10.4, pnpm 11.9.0, `shamefully-hoist=true` di `.npmrc`, `packageManager: "pnpm@11.9.0"` di root package.json (required by Turborepo 2.10+), `pnpm approve-builds` dijalankan untuk Prisma/sharp/msgpackr-extract. ESLint 9 Flat Config (`eslint.config.mjs`) + Prettier dikonfigurasi di root dengan pengecualian `.agents/` dan `docs/`. Port: API=3000, Notification=3001, AI-Gateway=3002, Web=4000, Admin=4001. Git config user.name & user.email dikonfigurasi lokal.                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Selesai.                                                                                                                                                                                                                                                                         |
| 1    | 2026-07-13 | Domain Identity & Access dan Customer selesai diimplementasikan. Prisma schema diupdate dengan model `User` (staff), `Customer`, `CustomerAuthMethod`, `Review`, dan `OtpCode`. Module `IdentityAccess` mengelola staff login, OTP HP, Google OAuth mock callback, dan `/auth/me`. Guard `AuthGuard` global mengontrol RBAC dan custom decorators `@Roles()`, `@AllowCustomer()`, dan `@Public()`. Module `Customer` memiliki endpoints CRUD profil customer, review, dan `/orders` stub. Event emitter digunakan untuk memicu domain events `CustomerRegistered` dan `CustomerProfileUpdated`. Unit tests (12 tests) untuk `AuthService` dan `CustomerService` passed 100%. Seed script ditambahkan untuk staff dan mock customers.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | JWT Sign & Verification di dalam package `@mlv/auth`. Password & OTP hashing menggunakan bcryptjs. Definite assignment assertions (!) ditambahkan pada DTO properties untuk class-validator compatibility. Global `ValidationPipe` diaktifkan di NestJS API. Jest dikonfigurasi menggunakan preset `ts-jest` di sub-package `@mlv/api`. Port API=3000, ports web/admin tetap.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Google OAuth masih mock callback, perlu kredensial asli sebelum Fase 10 (Customer Portal go-live). Menunggu konfirmasi user sebelum lanjut ke Fase 2: Inventory Domain Fondasi.                                                                                                  |
| 2    | 2026-07-13 | Inventory Domain fondasi diimplementasikan. Skema Prisma mencakup Material, BillOfMaterial, Warehouse (1 default), StockBalance (cache), StockReservation, StockMovement (sumber kebenaran), PurchaseOrder (CRUD dasar), dan StockAdjustment. Logic reservasi menggunakan row-locking PostgreSQL ('FOR UPDATE') untuk mencegah overselling. Ditambahkan endpoint API (/materials, /bom, /stock/reserve, /stock/release, /stock/movements, /stock/balance, /purchases, /stock/adjustments) dengan RBAC internal staff (Owner, Manajer, Penjahit). Event internal emitter diaktifkan untuk StockReserved, StockReservationReleased, StockDeducted, dan StockLow. Unit & integration tests untuk Inventory (10 tests: 9 unit + 1 concurrency integration) passed 100% baik lokal maupun di CI, membuktikan row-locking berhasil mencegah overselling.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        | PostgreSQL row-locking 'FOR UPDATE' diimplementasikan via prisma.$queryRaw di dalam transaksi interaktif prisma.$transaction. Event emitter internal modular monolith menggunakan `@nestjs/event-emitter`. DTOs menggunakan definite assignment assertion (!) untuk class-validator. Integrasi testing menggunakan database Postgres docker riil pada port 5433 (lokal) dan port 5432 (CI). Alur migrasi beralih ke prisma migrate dev. CI dikonfigurasi dengan PostgreSQL 18 & Redis 8 service containers. Migrasi & seeder dijalankan otomatis sebelum test di CI. Skip logic diperketat (fail keras jika DB tidak terhubung, kecuali disetel SKIP_DB_INTEGRATION=true). Flag `passWithNoTests` ditambahkan hanya untuk package `@mlv/notification` dan `@mlv/ai-gateway` karena statusnya masih skeleton (belum ada test).                                                                                                                                                                                                          | CI hijau terverifikasi penuh (22/22 test lulus, termasuk concurrency integration test). Menunggu keputusan teknis format Order ID, workflow reservasi, dan master data BOM tambahan sebelum lanjut ke Fase 3: Order Domain.                                                      |
| 3    | 2026-07-13 | Order Domain selesai diimplementasikan. Skema Prisma mencakup Order (dengan `order_number` MLV-YYYYMMDD-XXXX), OrderItem, OrderSize, OrderDesign, OrderMaterial (plain FK ke Material, cross-domain), OrderService, dan OrderTimelineEvent. OrderService mengimplementasikan: CRUD order, tambah item, upload desain (local disk), layanan tambahan, checkout flow dengan kalkulasi BOM otomatis, atomic stock reservation (jika satu gagal, semua di-rollback), pembatalan order, dan repeat order. Semua endpoints �8 diimplementasikan dengan RBAC sesuai �5.1 (@AllowCustomer + filter customer_id). CustomerService.findOrders() diupdate dari stub menjadi query nyata ke Order Domain. Seed script diperbarui dengan BOM placeholder untuk Kaos, Kemeja, Hoodie, Topi, dan Tas (termasuk material baru: Kancing, Tali Hoodie, Tali Tas). Unit tests (16 tests) untuk OrderService passed 100%. Total tests sekarang 38/38 pass.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                    | Kommunikasie Order?Inventory Domain selalu lewat InventoryService method (DDD boundary �4.1), bukan query Prisma langsung. `OrderMaterial.material` adalah plain foreign key tanpa opposite relation (sama pattern dengan `StockReservation`). `order_number` adalah human-readable identifier, BUKAN primary key/FK. Reservasi terjadi saat checkout (status DRAFT?MENUNGGU_PEMBAYARAN_DP), bukan saat tambah item. Atomic reservation: jika satu material gagal reserve, semua yang sudah ter-reserve di-release. Upload desain pakai local disk (`uploads/designs/`), siap di-swap ke S3-compatible storage nanti. BOM placeholder ditandai jelas di seed script untuk review dengan data produksi asli.                                                                                                                                                                                                                                                                                                                            | Bukti atomik reservasi perlu diverifikasi manual/end-to-end (test concurrency khusus). Production Domain (Fase 4) baru bisa mulai setelah keputusan: bagaimana mapping product_type ? production_routings? Apakah setiap product type punya routing??, atau dinamis per order? |
| 4    | 2026-07-14 | Production Domain selesai diimplementasikan. Skema Prisma mencakup `production_routings` (template routing per product_type) dan `production_tasks` (task granular per order_item). Routing template di-seed untuk 5 product type (Kaos, Kemeja, Hoodie, Topi, Tas). OrderConfirmed event listener (internal emitter) otomatis generate tasks sesuai routing saat order masuk ANTREAN. Task pertama status=DITERIMA, sisanya MENUNGGU. Printing/Embroidery task di-skip jika order_item tidak punya service sablon/bordir. TaskCompleted event men-trigger task berikutnya (status ? DITERIMA). ProductionCompleted event di-publish saat semua tasks selesai. RBAC diimplementasi: Owner/Manajer full access, Tim Penjahit hanya bisa update task miliknya sendiri. New test: atomic rollback test membuktikan material ke-1 & ke-2 benar-benar di-release saat material ke-3 gagal di-reserve. Total tests sekarang 41/41 pass. **Refactor DDD boundary**: Production module tidak boleh akses `prisma.orderTimelineEvent` langsung � semua pencatatan timeline ke Order Domain harus lewat `OrderService.addTimelineEvent()`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | Routing fixed per product_type (bukan dinamis per order). Trigger task generation = event OrderConfirmed (dari checkout, bukan PaymentSucceeded langsung). production_routings adalah tabel nyata (konsisten dengan pola BOM). Task Printing/Embroidery di-skip jika tidak ada service terkait. **DDD boundary tegas**: Production module TIDAK BOLEH akses tabel Order Domain (`order_timeline_events`) langsung � harus lewat OrderService. Ini berlaku SEKARANG (modular monolith), bukan hanya saat pisah microservice.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Menunggu keputusan sebelum Fase 5: Finance Domain.                                                                                                                                                                                                                               |
| 5    | 2026-07-14 | Finance Domain selesai diimplementasikan. Skema Prisma mencakup Payment (DP/Pelunasan), Invoice, ProfitSharing, dan Approval. Midtrans Snap API integration dengan signature verification dan idempotency check via webhook_event_id. Event-driven integration: PaymentSucceeded ? Order transition (DP?Antrean+OrderConfirmed, Pelunasan?Lunas). ProductionCompleted ? auto-generate Pelunasan invoice. Approval workflow dengan 4 tipe (HARGA_KHUSUS, DISKON, EDIT_INVOICE, REFUND) � semua efek ke domain lain lewat service method. RBAC: Owner approve/reject, Manajer ajukan saja. ProfitSharing Owner-only. **Bug fix**: auth/types/db packages export dari `./dist/` bukan `./src/` untuk fix ERR_MODULE_NOT_FOUND saat `pnpm dev`. **Webhook signature verification tests**: 7 tests baru untuk invalid signature rejection, idempotency check, duplicate webhook handling. **CI hijau terverifikasi** (run [29383468334](https://github.com/mlvweardev-source/mlv/actions/runs/29383468334), commit `be81bf2`): Test Suites 7/7 pass, Tests **56/56 pass** � semua step CI (Build turbo ? Migrate & Seed turbo ? Lint ? Test) success. Lint errors (152) dibereskan via `prettier --write` + konversi `require('crypto')` ? ES import di finance.service.ts & finance.webhook.spec.ts; 113 warnings (unused vars/any) dibiarkan sebagai technical debt untuk Fase 14.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           | DP custom per order (jumlah eksplisit dari staff). Midtrans sandbox integration. Invoice auto-generate saat payment dibuat (DP) dan ProductionCompleted (Pelunasan). Semua modifikasi lintas domain (Order, Inventory) lewat service method. Signature verification webhook dengan SHA512 hash. Approval effects: HARGA_KHUSUS?OrderService.overrideItemPrice, DISKON?OrderService.applyDiscount, EDIT_INVOICE?OrderService.reissueInvoice, REFUND?OrderService.releaseReservationsForOrder+cancelOrderByFinance.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                      | Menunggu keputusan sebelum Fase 6: Event-Driven Integration Layer (Redis + BullMQ).                                                                                                                                                                                              |
| 6    | 2026-07-15 | Event-Driven Integration Layer (Redis + BullMQ) selesai. **Seluruh event domain migrasi dari `@nestjs/event-emitter` (in-process) ke BullMQ** � tidak ada dua sistem event paralel; `EventEmitterModule` dihapus dari AppModule. Kontrak event terpusat di `packages/types/src/events.ts`: `QUEUES` (5 queue, satu per domain konsumen sesuai �4), `EVENT_NAMES` (job name = nama event), `EVENT_ROUTING` (event ? queue konsumen; Notification = subscriber umum, menerima SEMUA event), `EVENT_JOB_OPTIONS` (retry 3x exponential backoff 1s?2s?4s, `removeOnFail: false` = failed state jadi DLQ). Producer: `EventBusService` (@Global `EventBusModule`) di services/api. Consumer: 4 processor BullMQ (`OrderEventsProcessor`, `InventoryEventsProcessor`, `ProductionEventsProcessor`, `FinanceEventsProcessor`) menggantikan semua listener EventEmitter2 lama (file listener dihapus). `services/notification` = proses TERPISAH yang subscribe `notification-events` � **bukti lintas proses terverifikasi**: log worker menunjukkan `[CROSS-PROCESS] Event diterima dari services/api via Redis: payment.succeeded` dst. **Idempotency konsumen (�16) via cek state DB** (bukan dedup BullMQ): Order cek status order, Production cek count tasks per order item, Finance cek invoice PELUNASAN existing, Inventory hanya proses reservasi ACTIVE. Bull Board di `/admin/queues` (services/api) dengan HTTP Basic Auth terpisah (env `BULL_BOARD_USER`/`BULL_BOARD_PASSWORD`; fail-closed jika password kosong). **Demo cascade end-to-end LULUS** (`services/api/scripts/demo-cascade.ts`, `pnpm --filter @mlv/api demo:cascade`): PaymentSucceeded(DP) ? order-events (order ? ANTREAN) ? OrderConfirmed ? production-events (6 task Kaos: CUTTING?PRINTING?SEWING?FINISHING?IRONING?PACKING) + inventory-events (5 reservasi ? CONSUMED via stock_movements OUT) + notification-events (14 job diterima proses terpisah) � job counts: order-events 1, inventory-events 1, production-events 6, finance-events 1, notification-events 14, failed 0. **Bukti idempotency**: PaymentSucceeded + OrderConfirmed duplikat di-publish ulang ? status/timeline/tasks TIDAK berubah (log: "idempotent no-op"). **Bug arsitektur di-fix**: OrderConfirmed TIDAK lagi dipublish saat checkout (pra-Fase 6 dipublish dobel: checkout DAN DP sukses � menyebabkan deduction+task generation sebelum bayar); sekarang hanya saat DP sukses (�7.2). **Latent bug Fase 5 ditemukan & di-fix**: skema Finance tidak pernah dibuatkan migration (CI hijau karena test Finance semua mock Prisma) ? migration `20260715050230_add_finance_domain` dibuat. Test: 56 lama tetap lulus (di-update mock EventEmitter2?EventBusService) + 21 baru (routing EventBus 5, idempotency Order 7, Production 3, Finance 3, Inventory 3) = **77/77 pass, 12 suites**, termasuk concurrency integration vs Postgres riil. `pnpm turbo build` 10/10 sukses. | Satu queue per domain KONSUMEN (bukan per event type): `order-events`, `inventory-events`, `production-events`, `finance-events`, `notification-events`. Job name = nama event, payload = event object. Retry 3x exponential backoff; failed state BullMQ = DLQ (tanpa tabel custom), monitor via Bull Board. Publish event SETELAH `prisma.$transaction` commit (bukan di dalam) � event tidak boleh terkirim untuk transaksi yang rollback. `PaymentSucceeded` ? Order saja (bukan langsung ke Production/Inventory); Order menerbitkan `OrderConfirmed` ? dikonsumsi Production (generate tasks), Inventory (kunci reservasi ? deduction �7.2). `StockReservationFailed` terdaftar di routing tapi belum ada publisher (wajar, �4). Scheduled/repeatable jobs ditunda ke Fase 11. `@mlv/types` jadi dependency `services/notification` (kontrak queue/event shared lintas proses). Bull Board fail-closed. `ProductionCompleted` ? order-events juga (status order ? MENUNGGU_PELUNASAN, handler baru `handleProductionCompleted`). | CI hijau + link run perlu diverifikasi setelah push (standar wajib) � commit `caa65ff`.                                                                                                                                                                                          |
| 7    | 2026-07-15 | Shipping Domain selesai diimplementasikan. **Kriteria �23 terpenuhi: "Status pengiriman terhubung ke event pelunasan."** Model `Shipment` di Prisma: id, order_id (unique � 1 shipment per order), kurir, no_resi, status (DICATAT/DIKIRIM/DALAM_TRANSIT/DITERIMA), alamat_pengiriman (override opsional), biaya_kirim (informasional saja), tracking_token (UUID unik, auto-generate). Dua migrasi: `20260715065939_add_shipping_domain` (tabel shipments + notification_templates + notification_logs) dan `20260715070607_add_order_customer_relation` (relasi Order?Customer untuk `getOrderByIdInternal`). Event baru di `@mlv/types`: `ShipmentCreated` ? [order-events, notification-events]; `ShipmentDelivered` ? [order-events, notification-events]. `ShippingModule`: `ShippingService`, `ShippingController`, import `OrderModule` (DDD boundary). **GATE LUNAS**: `POST /shipments` ditolak dengan `400 "Order belum berstatus LUNAS"` jika order belum lunas � validasi via `OrderService.getOrderByIdInternal()` (bukan query tabel Order langsung). **Cascade via BullMQ**: `ShipmentCreated` ? `OrderEventsProcessor.handleShipmentCreated()` ? order status LUNAS?DIKIRIM + timeline event. **Public tracking (�8 + keputusan #4)**: `GET /shipments/:token/track` � token UUID unik per shipment, BUKAN orderId polos; response minimal (orderNumber, status label, kurir, noResi, tanggal) tanpa data sensitif (harga/alamat/customerId); token salah ? 404. **DDD boundary dijaga ketat**: ShippingService TIDAK pernah query tabel orders/customers langsung � semua via `OrderService.getOrderByIdInternal()`. **Dead-code dihapus**: `shipping-events.processor.ts` (processor dengan `@Processor(ORDER_EVENTS)` yang tidak diregister � berbahaya karena worker kedua mencuri job dari OrderEventsProcessor). **RBAC**: Owner & Manajer Produksi full akses staff endpoints; Tim Penjahit tidak ada akses; tracking publik tanpa auth. **Demo script**: `pnpm --filter @mlv/api demo:shipping` � membuktikan 3 hal: (1) gate LUNAS, (2) cascade DIKIRIM via BullMQ, (3) tracking publik HTTP tanpa auth. **Tests**: 109/109 pass (13 suites) � termasuk 17 test ShippingService (gate, DDD boundary, idempotency, public tracking), 4 test handleShipmentCreated idempotency di order-consumer.idempotency.spec.ts, 2 test routing ShipmentCreated/ShipmentDelivered di event-bus.service.spec.ts. `tsc --noEmit` clean, `pnpm turbo build` 10/10. Commit: `ccf6279`.                                                                                                                                                                                                                                                                                                                                                                                                                                           | Resi MANUAL (bukan integrasi API kurir riil). biaya_kirim INFORMASIONAL saja (tidak diintegrasikan ke Finance/invoice). Tracking publik via token UUID unik (bukan orderId polos). `OrderService.getOrderByIdInternal()` = cross-domain read method (DDD �4.1) � mengembalikan {id, status, orderNumber, customerId, alamat} tanpa expose DTO response. Relasi `Order?Customer` ditambahkan ke schema untuk mendukung `select customer { alamat }` di `getOrderByIdInternal`. `ShipmentCreated` ? order-events (consumer: OrderEventsProcessor) + notification-events (subscriber umum). Tidak ada processor Shipping Domain di queue manapun (Shipping = publisher murni �4).                                                                                                                                                                                                                                                                                                                                                         | CI hijau + link run perlu diverifikasi setelah push (standar wajib) � commit `ccf6279`. Menunggu keputusan sebelum Fase 8: Notification Domain.                                                                                                                                  |

| 8    | 2026-07-15 | Notification Domain selesai diimplementasikan. **Kriteria �23 terpenuhi: "Menambah channel baru tidak mengubah domain penerbit event."** Payload event diperkaya di sisi publisher (Finance: customerNama+customerNoHp; Inventory: materialNama; Production: customerNama+customerNoHp; Shipping: customerNama+customerNoHp+noResi; Approval: requestedByNama+decidedByNama) � `CustomerService.getCustomerByIdInternal()` + `AuthService.getUserByIdInternal()` cross-domain reads; Notification TIDAK pernah memanggil balik domain lain (prinsip Fase 8). Model: `notification_templates` + `notification_logs` (extended: eventType + dedupKey + index dedupKey+userId). Migration `20260715125526_add_notification_log_event_type_dedup`. **Arsitektur channel generik**: `NotificationChannel` interface + `FonnteChannel` (WA) + `DashboardChannel` � swap provider tanpa ubah domain lain. **DispatcherService**: template lookup ? render ({{placeholder}} dengan id-ID number format) ? send via channel ? log SENT/FAILED. Idempotency via dedupKey (sha256). **RBAC**: AuthGuard sendiri di proses notification (JWT shared via env); Owner/Manajer full access, Penjahit hanya miliknya. **Endpoint �8**: `GET /notifications` (list dengan filter channel/status, 100 terbaru) + `POST /notifications/dispatch` (manual trigger, Owner/Manajer). **7 templates seeded**: payment.succeeded/invoice.issued/shipment.created/production.completed (WA) + stock.low/approval.requested/approval.decided (Dashboard). **Demo 24 bukti lulus** (`pnpm --filter @mlv/notification demo:notification`): WA sandbox log (300.000 format), Dashboard log, idempotency (jumlah log tetap 1 saat re-dispatch), GET /notifications dengan JWT Owner, RBAC Penjahit, payload lengkap ShipmentCreated+ProductionCompleted. **Tests**: 110/110 pass (14 suites) � 109 api lama tetap pass (provider mock added) + 1 notification unit test (renderTemplate). **CI hijau terverifikasi** (run [29429791482](https://github.com/mlvweardev-source/mlv/actions/runs/29429791482), commit `9b48b6b`) � commit `d3bc409` (kode Fase 8) sempat merah karena prettier formatting (CRLF/OneDrive), di-fix di `9b48b6b` tanpa perubahan logic. | **Payload enrichment prinsip**: `PaymentSucceededEvent` (8 params), `InvoiceIssuedEvent` (+orderNumber+customerNama+customerNoHp), `ShipmentCreatedEvent` (+noResi+customerNama+customerNoHp), `ProductionCompletedEvent` (+customerNama+customerNoHp), `StockLowEvent` (+materialNama), `ApprovalRequestedEvent` (+requestedByNama), `ApprovalDecidedEvent` (+decidedByNama). Kontrak di `packages/types/src/event-payloads.ts`. Fonnte sandbox: jika `FONNTE_API_TOKEN` kosong ? log only, dianggap sukses (dev/CI/demo). `getOrderByIdInternal()` sudah ada Fase 7; ditambah `getCustomerByIdInternal()` (CustomerService) + `getUserByIdInternal()` (AuthService) di Fase 8. Channel baru (Email/Push/WA resmi) = class implement `NotificationChannel` + daftar di module factory � 0 baris change di domain lain. | **Keputusan sebelum Fase 9 � Portal Internal**: (1) Portal wujud: admin panel Next.js (`apps/admin`) atau endpoint API + UI terpisah? (2) Notification Center UI: tampilkan per-channel atau unified? (3) Email channel: prioritas rendah, boleh Fase 11+. (4) Push notification: perlu service worker registration � masuk Fase 10 atau 11? |
| 9.1  | 2026-07-16 | **Fase 9 Bagian 1 selesai: Auth/RBAC shell + Dashboard stub + Modul Order UI** (portal internal `apps/admin`, Next.js 16.2.10 + Tailwind v4 + komponen gaya shadcn/ui). **Identity & Access � refresh token**: model `RefreshToken` (migration `20260715171550_add_refresh_token`) � token disimpan SHA-256 hash, revocable via `revokedAt`. `POST /auth/login` set access token (~1 jam) + refresh token (~7 hari) sebagai **httpOnly cookie** (`mlv_access_token` + `mlv_refresh_token`) � body response HANYA berisi info user, TANPA token (mitigasi XSS). `POST /auth/refresh`: rotasi token (lama di-revoke, pasangan baru diterbitkan); **reuse detection**: refresh token yang sudah dirotasi dipakai ulang ? revoke SEMUA sesi user (fail-closed). `POST /auth/logout`: revoke refresh token di DB + clear cookies. `AuthGuard.extractToken()` membaca Bearer header ATAU cookie. `main.ts`: cookie-parser + CORS credentials (origin 4000/4001, env `CORS_ORIGINS`) + static `/uploads`. **RBAC �5.1 Order**: `GET /orders` + `GET /orders/:id` + `GET /orders/:id/timeline` kini boleh diakses `TIM_PENJAHIT` tapi **view terbatas** � hanya order yang punya production task assigned ke dirinya, via `ProductionService.getOrderIdsForAssignee()` (DDD �4.1, circular module Order?Production diselesaikan dengan `forwardRef`); order lain ? 403. `findOrders` dapat filter `?status=` + `?search=` (order number, case-insensitive). **Portal shell**: `proxy.ts` (Next.js middleware/Edge) � belum login ? redirect `/login`; access token expired + refresh token ada ? **auto-refresh server-side** (fetch `/auth/refresh`, teruskan Set-Cookie); role tidak berhak ? redirect `/403` (matriks ROUTE_ROLES per �5.1); decode payload TANPA verify di edge hanya untuk routing UX � enforcement data tetap di API. Login page cookie-based (JS tidak pernah menyentuh token). Sidebar dengan menu RBAC-filtered + badge "Soon" untuk modul belum dibangun; Topbar nama user (dari `/auth/me` server-side) + tombol logout. Dashboard stub "Dashboard Analytics" (metrik Fase 13). **Modul Order UI**: `/orders` (tabel, filter status, search debounce 300ms), `/orders/[id]` (items: sizes/designs/materials BOM/services + timeline kronologis), aksi role-based (`canAct` = Owner/Manajer): update status (DRAFT?MENUNGGU_PEMBAYARAN_DP?ANTREAN, batalkan), upload desain (file input ? multipart `POST /orders/:id/items/:itemId/designs`), repeat order (`POST /orders/:id/duplicate` ? redirect ke order baru). **Demo 24 bukti lulus** (`pnpm --filter @mlv/api demo:auth-portal`): login cookie httpOnly, body tanpa token, /auth/me + /orders via cookie, tanpa cookie 401, Penjahit lihat 1 dari 2 order (miliknya saja), buka order lain 403, PATCH status ditolak, access expired 401 ? refresh sukses tanpa login ulang ? token dirotasi ? reuse token lama 401, logout ? refresh 401. **Tests**: **116/116 pass (13 suites)** � 14 test baru refresh/logout/rotasi/reuse-detection di auth.service.spec.ts. `pnpm turbo build` 10/10, `next build` sukses (proxy terdeteksi), prettier + lint hijau. Commit `9040841`. | Access token staff ~1h (`JWT_ACCESS_EXPIRES_IN`), refresh ~7d (`REFRESH_TOKEN_EXPIRES_DAYS`), keduanya httpOnly `sameSite:lax`, `secure` di production. Refresh token di-hash SHA-256 sebelum disimpan (leak DB tidak bocorkan token hidup). Rotasi setiap refresh + reuse detection revoke semua sesi. Kedua cookie path `/` supaya middleware admin bisa auto-refresh. Customer OTP/Google flow TIDAK berubah (masih Bearer body � cookie hanya untuk staff portal). Di `apps/admin`: `@/*` path ? root app (bukan `src/`), Tailwind v4 via `@tailwindcss/postcss` + `@theme` (bukan tailwind.config), komponen shadcn-style ditulis manual (button/input/card/badge/table/select) tanpa CLI. File Next 16: `proxy.ts` (bukan `middleware.ts`). RBAC di UI 3 lapis: sidebar menyembunyikan menu, proxy redirect 403, API tetap enforcement final. `NEXT_PUBLIC_API_URL` (client) + `API_INTERNAL_URL` (server/edge) default `http://localhost:3000`. Transisi status di UI dibatasi yang sudah diimplementasi backend: DRAFT?DP, DP?ANTREAN, ?DIBATALKAN. eslint warnings 113?115 (2 baru di spec, teknis debt Fase 14 seperti keputusan Fase 5). | CI hijau + link run diverifikasi setelah push � commit `9040841`. **Menunggu konfirmasi user untuk lanjut Fase 9 Bagian 2: Modul Production & Inventory.** Keputusan pending: (a) Production UI: kanban board per task type atau tabel flat? (b) Inventory UI: apakah purchase order workflow masuk Bagian 2 atau nanti? |
| 9.2  | 2026-07-15 | **Fase 9 Bagian 2 selesai: Modul Production UI + Inventory UI** (`apps/admin`). **Keputusan user dieksekusi**: Production UI role-based � Owner/Manajer = **kanban board** (kolom per task_type urutan �25.1: Cutting ? Printing ? Embroidery ? Sewing ? Finishing ? Ironing ? Packing; kartu = task individual dengan order number, product type, assignee, status badge; counter per kolom memerah saat =5 task menumpuk = bottleneck kelihatan sekilas; klik kartu ? panel detail dengan aksi assign + update status; checkbox "tampilkan yang selesai"); Tim Penjahit = **tabel flat** task miliknya saja dengan tombol update status per baris (backend GET /production/tasks sudah auto-filter `assignedTo=sub` untuk role TIM_PENJAHIT sejak Fase 4 � UI tidak kirim param apa pun). Drag-drop TIDAK dibangun (nice-to-have; klik tombol di kartu sudah cukup sesuai arahan). **Inventory UI 5 tab** (`/inventory/*` dengan layout tab bersama): `/stock` (stock_balances: tersedia/direservasi/bebas dipakai + badge "Menipis" < 5), `/materials` (list + form tambah, Owner/Manajer saja � Penjahit view-only �5.1), `/bom` (read-only per product type, card grid), `/adjustments` (list + form qty_delta�/alasan, Owner/Manajer), `/purchases` (list + form buat PO + tombol **"Tandai Diterima"**). **BUG FUNGSIONAL DITEMUKAN & DI-FIX (backend)**: `completePurchaseOrder` TIDAK ADA sama sekali dari Fase 2 (PO hanya bisa dibuat PENDING, tidak ada jalan ke COMPLETED) � dibangun `PATCH /purchases/:id/complete`: dalam SATU transaksi compare-and-swap `updateMany({id, status:'PENDING'})` (klik ganda/paralel tidak bisa menambah stok 2�) + `stock_movements` tipe `IN` (refType `purchase_order`) + `stock_balances.qty_available` bertambah via pola row-lock FOR UPDATE yang sama dengan createStockMovement. **Endpoint backend baru lainnya**: `GET /purchases` (list + material), `GET /stock/adjustments` (list + material), `GET /bom` (semua BOM, di-group per productType di frontend), `GET /auth/users?role=` (Owner/Manajer � dropdown assign penjahit; bukan manajemen user penuh), `@Roles(OWNER, MANAJER_PRODUKSI, TIM_PENJAHIT)` class-level di ProductionController (sebelumnya TANPA @Roles ? customer ber-JWT valid lolos guard global). **Refactor kecil**: `requireRole()` helper server component (`apps/admin/lib/server-auth.ts`) menggantikan pola decode-cookie berulang. **Demo 16 bukti lulus** (`pnpm --filter @mlv/api demo:production-inventory`): (1) kanban Manajer � task tersebar 4 kolom, PATCH CUTTING?SELESAI memicu SEWING?DITERIMA (kartu pindah kolom); (2) assign ke penjahit; (3) Penjahit GET tasks ? 1 dari 4 (miliknya saja), PATCH task orang lain 403; (4) **PO ? tandai diterima ? stock_balance Kain 954?979 (+25 NYATA) + movement IN tercatat + complete kedua kali 400 tanpa penambahan dobel**; (5) RBAC Penjahit: POST /materials 401, GET /stock/balance 200, complete PO 401. **Tests: 123/123 pass (13 suites)** � 6 test baru inventory (completePurchaseOrder sukses/404/idempoten/init-balance, findPurchaseOrders, findStockAdjustments, findAllBoms). `pnpm turbo build` 10/10, prettier --check + `pnpm lint` (eslint & tsc 13/13) hijau. | Kolom kanban = `task_type` (7 kolom tetap �25.1), BUKAN status � bottleneck per tahap terlihat dari tumpukan kartu. Threshold merah counter kolom = 5 task (sinkron dengan LIMIT StockLow). Transisi task di UI mengikuti validasi backend persis: MENUNGGU?SEDANG_DILAKSANAKAN; DITERIMA?SEDANG_DILAKSANAKAN\|SELESAI; SEDANG_DILAKSANAKAN?SELESAI. `PATCH /purchases/:id/complete` = compare-and-swap PENDING?COMPLETED di dalam transaksi (BadRequest "sudah ditandai diterima" saat status bukan PENDING) � idempotency guard konsisten prinsip �16. PO complete TIDAK publish event BullMQ (aksi HTTP sinkron, bukan konsumsi event; StockLow tetap dari deduction path). `GET /auth/users` menyaring `isActive: true` + validasi enum role query param. Sidebar: Production & Inventory tidak lagi "Soon". ROUTE_ROLES `/production` & `/inventory` sudah benar dari Bagian 1 (ketiga role � Penjahit view-only ditegakkan per-halaman via `canAct` + API). Edit BOM di UI = di luar scope (read-only view; createBom endpoint sudah ada sejak Fase 2 kalau nanti dibutuhkan). | CI HIJAU TERVERIFIKASI (run [29466399437](https://github.com/mlvweardev-source/mlv/actions/runs/29466399437), commit `5a510ae`) � semua step (Build turbo ? Migrate & Seed ? Lint ? Test 123/123) success. **Menunggu konfirmasi user ("lanjut") untuk Fase 9 Bagian 3: Modul Finance, Approval, & Shipping.** |
| 9.3  | 2026-07-16 | **Fase 9 Bagian 3 selesai: Modul Finance, Approval, & Shipping UI** (`apps/admin`). **Payment & Invoice**: section Pembayaran di `/orders/[id]` (riwayat payment DP/Pelunasan + status badge, invoice + tombol unduh PDF via `GET /invoices/:id/pdf`, tombol **"Buat Link Pembayaran"** � staff input jumlah eksplisit karena DP custom per order [keputusan Fase 5], `POST /payments` metode `midtrans_snap` ? URL redirect Snap sandbox ditampilkan + tombol salin untuk dibagikan ke pelanggan); halaman `/finance` layout tab (pola Inventory 9.2): tab Payment & Invoice sistem-wide **view-only** (tanpa tombol buat � �5.1 Manajer view; buat payment cukup dari halaman Order), tab **Bagi Hasil hanya dirender untuk Owner**. **Approval (�13)**: dialog "Ajukan Approval" di `/orders/[id]` (tipe Harga Khusus/Diskon/Edit Invoice/Refund; referensi dinamis per tipe � HARGA_KHUSUS?dropdown item, EDIT_INVOICE?dropdown invoice, DISKON/REFUND?order itu sendiri; catatan; `POST /approvals`); halaman `/approvals` = inbox � Owner: semua request, filter pending/decided, kolom input catatan + tombol Approve/Reject (`PATCH /approvals/:id/decide`); Manajer: **hanya status request yang dia ajukan sendiri** (difilter BACKEND `where.requestedBy = actor.sub`, bukan cuma UI), tanpa tombol decide. **Profit Sharing**: `/finance/profit-sharing` CRUD **Owner-only** � RBAC 3 lapis penuh: entri ROUTE_ROLES `{prefix: '/finance/profit-sharing', roles: ['OWNER']}` ditempatkan **di ATAS** `/finance` (allowedRolesFor = first-match prefix!) ? proxy redirect /403; server component cek role lagi; API `@Roles(OWNER)` + cek service. **Shipping**: tombol "Kirim Pesanan" di `/orders/[id]` **hanya enabled saat status LUNAS** (disabled + keterangan "Order harus berstatus Lunas dulu" kalau belum; hilang kalau sudah DIKIRIM) � form kurir + resi opsional + alamat override + biaya kirim (informasional) ? `POST /shipments`, tracking token ditampilkan setelah sukses; halaman `/shipping` daftar semua shipment (Owner & Manajer per �5.1) + dropdown update status maju (DICATAT?DIKIRIM?DALAM_TRANSIT?DITERIMA) + salin token tracking. **FIX KEAMANAN BACKEND (ditemukan saat baca controller Fase 5)**: Payment/Approval/ProfitSharing controller membaca actor dari **header `x-user` (JSON.parse) yang bisa dipalsukan siapa pun ber-token valid** � diganti `@GetUser()` (payload JWT terverifikasi AuthGuard) + `@Roles` class-level (payments/invoices/approvals: OWNER+MANAJER_PRODUKSI; profit-sharing: OWNER). **Endpoint baru**: `GET /payments?orderId=`, `GET /invoices?orderId=`, `getApprovals` menerima actor (filter Manajer) + enrich `requesterNama`/`approverNama` via `AuthService.getUserByIdInternal` (DDD �4.1), `CreateApprovalDto.orderId` (konteks order di inbox). **Demo 25 bukti lulus** (`pnpm --filter @mlv/api demo:finance-shipping`): (1) POST /payments DP Rp 500rb ? link Snap sandbox nyata ter-generate + invoice DP otomatis; (2) Manajer ajukan DISKON ? inbox Owner PENDING + nama pengaju ? Owner REJECT (order tak didiskon) ? ajukan ulang ? APPROVE ? **efek nyata: `discount_nominal=50000` + timeline `DISKON_APPLIED`**; Manajer decide?403; Manajer list?miliknya saja; (3) profit-sharing: Manajer/Penjahit GET/POST?401; Owner create?list?update 15%?20%?delete semua jalan; (4) shipment saat ANTREAN?400, saat LUNAS?201 + trackingToken, **order otomatis DIKIRIM via event BullMQ** (di-poll), Penjahit?401. **Tests: 134/134 pass (14 suites)** � 11 test baru (`finance.list.spec.ts`): findPayments/findInvoices filter, getApprovals RBAC Owner-vs-Manajer + enrich nama, createApproval orderId, profit-sharing Forbidden. Build 10/10, prettier --check + pnpm lint hijau. | Buat payment = dari halaman Order (konteks), BUKAN dari /finance (�5.1 Manajer view-only di Finance � Owner pun tidak perlu tombol di sana). `ROUTE_ROLES` first-match: prefix lebih spesifik WAJIB di atas prefix umum. Webhook Midtrans tanpa UI (server-to-server); status payment di UI cukup refresh/reload � tidak ada realtime. Efek approval DISKON dieksekusi backend Fase 5 via `OrderService.applyDiscount` yang mem-parse catatan keputusan ("Rp 50000" / "10%") � placeholder input decide Owner memberi hint format ini. UpdateProfitSharingDto tidak menerima `periode` (kolom create-only). PDF invoice masih placeholder URL dari Fase 5 (TODO backend, bukan scope UI ini). Tracking publik `/shipments/:token/track` TIDAK dibangun (Customer Portal Fase 10). | CI HIJAU TERVERIFIKASI untuk kedua commit: kode `c6065a5` ? run [29480721746](https://github.com/mlvweardev-source/mlv/actions/runs/29480721746); docs `0bf80d1` ? run [29481135309](https://github.com/mlvweardev-source/mlv/actions/runs/29481135309). **Menunggu konfirmasi user ("lanjut") untuk Fase 9 Bagian 4: Notification Center, Activity Log, & Internal Chat.** |
| 9.4  | 2026-07-16 | **Fase 9 Bagian 4 selesai: Notification Center, Activity Log, & Internal Chat** (+housekeeping invoice PDF). **Housekeeping**: `GET /invoices/:id/pdf` SUNGGUHAN � placeholder Fase 5 diganti PDFKit (ringan, tanpa browser headless): `InvoicePdfService` render info order, pelanggan (nama+HP via `CustomerService.getCustomerByIdInternal` DDD �4.1), item (qty � harga), layanan, subtotal/diskon/total, jumlah tagihan, jenis DP/PELUNASAN + status; file ke `uploads/invoices/<invoiceId>.pdf` (pola upload desain Fase 3, `uploads/` kini masuk `.gitignore`), `pdf_url` disimpan di kolom existing, di-serve via static `/uploads`; tombol unduh PDF di UI 9.3 kini membuka `API_URL+pdfUrl`. **Activity Log (�6.8)**: model `activity_log` (actor_id, actor_role, deskripsi, entity_type, entity_id, created_at + index) � migration `20260716091430_add_activity_log_and_internal_chat` (sekalian internal chat). `ActivityLogService` di `services/api/src/common/activity-log/` sebagai **modul @Global (pola EventBusModule)** � SESUAI klarifikasi fase ini: infrastruktur cross-cutting, BUKAN bounded context; domain manapun panggil `log()` langsung (in-process, TIDAK lewat BullMQ); `log()` fail-safe (catch+logger.error, kegagalan log tidak menggagalkan aksi bisnis). Retrofit aksi penting: Order (checkout DRAFT?DP, ?ANTREAN manual, pembatalan, DP sukses & pelunasan via event processor dgn actor SYSTEM), Production (assign task + task SELESAI), Finance (approval APPROVED/REJECTED, dicatat ke entity Order bila ada orderId supaya tampil di riwayat order), Shipping (shipment dibuat + DITERIMA). `GET /activity-log` (@Roles Owner+Manajer; filter entityType/entityId/limit via DTO). **Internal Chat (�11)**: model `internal_chat_threads` (order_id unique) + `internal_chat_messages`; `GET/POST /orders/:id/internal-chat` + **SSE** `GET .../stream` (@Sse NestJS, Observable merge(pesan$, ping 25s)); subscriber in-memory Map per thread; RBAC �5.1: Owner/Manajer semua thread, Penjahit HANYA order dengan production task assigned ke dirinya (403). **Fix hasil self-review**: (1) `validateAccess` di endpoint SSE WAJIB di-await (tanpa await = authorization bypass); (2) N+1 query nama pengirim ? `AuthService.getUsersByIdsInternal()` (batch, 1 query). **UI admin**: `/notifications` (daftar dispatch + filter channel/status, fetch ke port 3001 `NEXT_PUBLIC_NOTIFICATION_URL`), `/activity-log` (Owner/Manajer, ROUTE_ROLES baru), `/orders/[id]` + section **"Riwayat Aktivitas"** (filter entityType=Order, terpisah dari Timeline, berdampingan) + **panel Chat Internal realtime** (EventSource `withCredentials`, auto-scroll, bubble kiri/kanan by senderId); sidebar Notifikasi & Chat tidak lagi "Soon". **services/notification**: cookie-parser + CORS credentials + guard baca cookie `mlv_access_token` (paritas dengan services/api) supaya portal admin bisa fetch langsung. **Demo `pnpm --filter @mlv/api demo:chat-activity` SEMUA LULUS**: checkout tercatat di activity log; reject approval tercatat; Penjahit GET /activity-log ditolak 401; payment DP ? invoice ? **PDF valid ter-generate & dibaca ulang: header MLV Wear, INVOICE DP (DRAFT), order number, pelanggan Budi (08123456789), Kaos 3�Rp 80.000=Rp 240.000, tagihan DP Rp 300.000**; chat GET/POST 2 role, thread persist, Penjahit akses order bukan miliknya 403, SSE stream stay-open. **Tests: 134/134 pass (14 suites)** � spec lama diupdate mock `ActivityLogService`+`InvoicePdfService`. Build 10/10, prettier --check + eslint 0 error + turbo lint 13/13 hijau. Commit `cfcf13b`. | Activity Log = modul @Global di `common/` (BUKAN packages/db � service NestJS butuh DI logger/prisma singleton apapun tetap dari @mlv/db). Panggilan `activityLog.log()` sinkron in-process by design (klarifikasi fase: JANGAN over-engineer via BullMQ). Actor SYSTEM (actorId null) untuk transisi otomatis event processor. Realtime chat = SSE per keputusan sebelumnya (bukan WebSocket): POST pesan biasa, stream hanya push; subscriber Map in-memory per proses (cukup untuk modular monolith 1 proses; kalau nanti multi-instance perlu Redis pub/sub). `@Sse()` NestJS + Observable; keep-alive ping komentar SSE tiap 25s. Penjahit boleh lihat riwayat aktivitas per-order via `GET /activity-log`? TIDAK � endpoint itu Owner/Manajer saja; section di /orders/[id] pakai endpoint yang sama sehingga Penjahit tidak melihat section itu terisi (apiFetch 401 ? tampil kosong; acceptable, �5.1 turunan paling ketat). PDF pakai font builtin Helvetica (tanpa file font eksternal). `uploads/` di-gitignore (file PDF demo sempat hampir ter-commit). Known minor debt (tercatat, tidak blocking): Subject SSE tidak di-complete saat client disconnect (NestJS @Sse tanpa teardown hook default � leak kecil per koneksi, acceptable utk portal internal), upsert thread tiap sendMessage (idempoten tapi 1 write ekstra), `/notifications` UI fetch client-side ke 3001 (butuh worker notification hidup). **UPDATE (fix commit `b2ee29d`, CI hijau run [29503289099](https://github.com/mlvweardev-source/mlv/actions/runs/29503289099))**: fetch lintas port DIHAPUS � audit Set-Cookie menunjukkan `mlv_access_token` host-only (tanpa Domain, SameSite=Lax, Path=/) sehingga fetch browser ke port 3001 hanya kebetulan jalan di localhost (cookie mengabaikan port) dan PASTI putus di production dengan host terpisah. Fix: route handler `apps/admin/app/api/notifications/route.ts` (proxy same-origin, pola proxy.ts) � browser hanya bicara ke origin admin; server baca cookie lalu forward Bearer ke `NOTIFICATION_INTERNAL_URL` (default localhost:3001). Terverifikasi E2E dengan 3 server hidup: tanpa cookie ? 307 login, dengan cookie ? 200 + 16 notifikasi. Bonus fix: `express` jadi dependency eksplisit @mlv/api (dipakai langsung di main.ts, sebelumnya hanya resolve via hoisting transitive � `node dist/main.js` gagal MODULE_NOT_FOUND). | CI HIJAU TERVERIFIKASI: run [29498882982](https://github.com/mlvweardev-source/mlv/actions/runs/29498882982) commit `cfcf13b` � completed success. **Menunggu konfirmasi user ("lanjut") untuk Fase 9 Bagian 5: Dashboard Analytics** (atau gabung ke Fase 13 sesuai roadmap � diputuskan saat itu). |
| 10.1 | 2026-07-17 | **Fase 10 Bagian 1 selesai: Auth Pelanggan + Portal Shell (`apps/web`)** � aplikasi BARU untuk pelanggan publik (bukan staf). **Housekeeping Google OAuth SUNGGUHAN (mock Fase 1 DICABUT TOTAL)**: `GoogleAuthService` baru (google-auth-library resmi) verifikasi `id_token` Google Identity Services � signature ke public key Google + audience harus `GOOGLE_CLIENT_ID` kita; **fail-closed**: client ID kosong/placeholder `mock_*` ? 503, TIDAK ada fallback mock tersembunyi. Matching akun by Google `sub` (bukan email): (1) `googleId` cocok ? login; (2) belum ada + email TERVERIFIKASI Google cocok dengan customer existing ? link akun (tambah auth method GOOGLE) � email unverified TIDAK dipakai matching (guard account-takeover); (3) tidak ada ? auto-create `customers` + `customer_auth_methods`. Grep bersih: `OTP MOCK\|mockGoogleId\|mock.mlv.dev` = 0 hasil di source. **OTP via WhatsApp (Fonnte) lewat queue**: event baru `auth.otp.requested` (`OtpRequestedPayload` di `@mlv/types`, routing ? `notification-events` SAJA) � `requestOtp` publish event via EventBusService setelah hash tersimpan; `services/api` TIDAK memanggil Fonnte langsung (boundary Fase 8). Template WA di-seed (`auth.otp.requested`/WHATSAPP: kode + masa berlaku + peringatan jangan bagikan). **Keamanan log**: Dispatcher menandai event SENSITIF � pesan OTP di `notification_logs` di-MASK (`\d{4,}` ? `******`) supaya staff pembuka Notification Center tidak bisa membajak login pelanggan; pesan utuh hanya terkirim ke penerima WA. **Cookie pelanggan httpOnly**: `mlv_customer_token` TERPISAH dari cookie staff (dev share host localhost); `otp/verify` + `google/callback` set cookie, body TANPA token (paritas pola staf 9.1); `POST /auth/customer/logout` clear cookie (JWT pelanggan stateless 7d, tanpa refresh token); AuthGuard extractToken membaca Bearer / `mlv_access_token` / `mlv_customer_token`. **Portal shell `apps/web`** (Tailwind v4 + komponen shadcn-style, pola `apps/admin`): layout header (nav Beranda/Pesan Sekarang/Riwayat Pesanan + status sesi via `GET /auth/me` + tombol Masuk/Keluar) + footer; landing (hero CTA "Pesan Sekarang" ? `/pesan`, grid 5 produk Kaos/Kemeja/Hoodie/Topi/Tas, CTA login); `/login` dua alur: OTP (input HP ? kirim ? input kode ? verifikasi; `?from=` redirect balik) dan tombol Google Identity Services (script GIS dimuat hanya jika `NEXT_PUBLIC_GOOGLE_CLIENT_ID` terisi); `/pesan` + `/pesanan` placeholder (Bagian 2 & 3); `proxy.ts`: hanya `PROTECTED_PREFIXES` (`/pesanan`, `/akun`) yang butuh sesi ? redirect `/login?from=`, tanpa auto-refresh & tanpa RBAC (beda dari admin, semua pelanggan setara). **Demo 19/19 bukti LULUS** (`pnpm --filter @mlv/api demo:customer-auth`, worker notification hidup): OTP request 201 ? job `auth.otp.requested` menyeberang proses via Redis (log worker `[CROSS-PROCESS]`) ? FonnteChannel SANDBOX log berisi kode nyata ? `notification_logs` SENT dengan kode MASKED; verify ? Set-Cookie httpOnly, body tanpa token, customer + auth method auto-create, `/auth/me` via cookie 200 / tanpa cookie 401; Google token palsu ? 401 `Wrong number of segments` (verifikasi Google riil berjalan), count customer TIDAK bertambah. **Tests: 140/140 pass (14 suites)** � 6 test baru (requestOtp publish event + kode=hash, verifyOtp cookie result, googleCallback: fail-closed/login by sub/link by verified email/tolak link unverified email/auto-create). Build 10/10 (web: 5 route + proxy terdeteksi), prettier --check + eslint 0 error + turbo lint 13/13 hijau. | Identifier Google = `sub` (stabil), BUKAN email. Link-by-email HANYA jika `email_verified===true`. `GOOGLE_CLIENT_ID` (backend, audience check) = `NEXT_PUBLIC_GOOGLE_CLIENT_ID` (frontend, render tombol GIS) � satu client ID Web application; authorized JS origin `http://localhost:4000`. Kode OTP plaintext hanya hidup di payload event + pesan WA; DB simpan bcrypt hash; log notifikasi MASKED (`SENSITIVE_EVENTS` set di DispatcherService). Event OTP TIDAK extends CustomerContactFields (customer bisa belum terdaftar saat request); field nomor dinamai `customerNoHp` supaya `resolveRecipient` generik tanpa kasus khusus. Cookie pelanggan `mlv_customer_token` maxAge = `JWT_EXPIRES_IN` (7d); publish event SETELAH `otpCode.create` (publish gagal ? pelanggan request ulang, kode kadaluarsa sendiri). `apps/web` TIDAK transpilePackages lagi tak perlu diubah; `@/*` path ? root app (pola admin). Fonnte tetap sandbox (FONNTE_API_TOKEN kosong = log-only) sesuai keputusan user demo ini; token asli tinggal diisi tanpa perubahan kode. **Gotcha env Next.js**: `NEXT_PUBLIC_*` di root `.env` monorepo TIDAK terbaca `apps/web` (Next hanya baca env dari direktori app-nya, beda dengan NestJS `envFilePath: ['../../.env']`) � solusi: `apps/web/.env.local` (ter-gitignore) berisi `NEXT_PUBLIC_GOOGLE_CLIENT_ID` + `NEXT_PUBLIC_API_URL`; untuk deploy nanti, set env via platform hosting. | CI HIJAU TERVERIFIKASI untuk kedua commit: kode `3e49fda` ? run [29554583203](https://github.com/mlvweardev-source/mlv/actions/runs/29554583203); docs `de6df25` ? run [29554991051](https://github.com/mlvweardev-source/mlv/actions/runs/29554991051) � semua step (Build turbo ? Migrate & Seed ? Lint ? Test 140/140) success. **Login Google diverifikasi MANUAL end-to-end di browser sungguhan (2026-07-17)**: user klik tombol GIS di `http://localhost:4000/login` ? popup Google ? akun `customers` baru ter-create otomatis (nama + email asli dari Google, `googleId` = sub) + `customer_auth_methods` tipe GOOGLE � bukti dari DB, bukan hanya script HTTP. **Menunggu konfirmasi user ("lanjut") untuk Fase 10 Bagian 2: Alur Pemesanan Baru (Checkout).** |
| 10.2 | 2026-07-18 | **Fase 10 Bagian 2 selesai: Alur Pemesanan Baru (Checkout)**. **Database**: model `ProductPriceList` di `schema.prisma` + migrasi `20260718015007_alter_product_price_list` + seed data placeholder harga (Kaos: Rp 85.000, Kemeja: Rp 120.000, Hoodie: Rp 150.000, Topi: Rp 45.000, Tas: Rp 60.000). **Backend Order**: endpoint `GET /orders/check-availability?productType=X&qty=Y` (@AllowCustomer) panggil `InventoryService.checkAvailability()` (menggunakan `qtyAvailable - qtyReserved` free stock dari BOM) secara internal; transaksi `executeCheckout` otomatis membaca `ProductPriceList` untuk mengisi `basePriceSnapshot` jika masih bernilai `0`. **Backend Finance**: `CreatePaymentDto.jumlah` dibuat opsional; `POST /payments` ditambahkan `@AllowCustomer` + validasi kepemilikan order di `createPayment`; auto-calculate DP 50% untuk customer. **Frontend web**: halaman `/pesan` (Order Builder dengan input kuantitas multi-ukuran, stock status real-time, estimasi harga, upload desain, dan konfirmasi manual) + `/pesan/bayar/[id]` (halaman tunggu polling status dan redirect Snap). **FIX KEAMANAN & GERBANG STATUS**: (1) Celah manipulasi harga ditutup penuh: `POST /orders/:id/items` untuk role `CUSTOMER` akan mengabaikan nilai `basePriceSnapshot` di request body dan langsung mengambil data harga asli secara server-side dari `ProductPriceList`; (2) Gerbang status diimplementasikan ketat: `createPayment` memblokir DP jika order bukan `MENUNGGU_PEMBAYARAN_DP` dan memblokir Pelunasan jika order bukan `MENUNGGU_PELUNASAN`. **E2E Integration Demo** (`pnpm --filter @mlv/api demo:checkout`): Script 10 langkah menguji ketersediaan stok, deteksi manipulasi harga, pemblokiran status DP/Pelunasan yang tidak valid, penolakan kepemilikan oleh customer lain (403), kalkulasi otomatis DP 50%, dan simulasi webhook sukses Midtrans untuk transisi status order ke `ANTREAN` lulus 10/10. **Tests**: 148/148 pass (15 suites) � termasuk unit test `addOrderItem` pricing check di `order.service.spec.ts` dan unit test status gates & DP 50% di `finance.customer-payment.spec.ts`. Build 10/10 sukses. | Harga dasar menggunakan tabel `product_price_list` di-seed sebagai placeholder. Kuantitas checkout dipantau ketersediaannya sebelum checkout dimulai dengan free stock (tersedia - ter-reservasi). Customer payment menghitung DP 50% secara otomatis di backend untuk mencegah manipulasi client-side, dan jumlah di DTO dibuat opsional. Untuk customer, basePriceSnapshot diisi server-side dari ProductPriceList di level `addOrderItem` guna menutup celah manipulasi harga. | CI HIJAU TERVERIFIKASI: run [29627225146](https://github.com/mlvweardev-source/mlv/actions/runs/29627225146) commit `f5bdb3d` � completed success. CI HIJAU TERVERIFIKASI: run [29627225146](https://github.com/mlvweardev-source/mlv/actions/runs/29627225146) commit f5bdb3d - completed success. |
| 10.3 | 2026-07-18 | **Fase 10 Bagian 3 selesai: Portal Existing-Customer**. **Backend Order**: logic duplikasi di `duplicateOrder` diperluas dengan mengambil harga dasar terupdate dari `ProductPriceList` dan menyalin desain terakhir serta service ke order baru berstatus `DRAFT`. Menambahkan API `PATCH /orders/:id/items/:itemId` (@AllowCustomer) untuk mengedit item draft. Di `uploadDesignWithUrl`, ditambahkan gate status cutting via `ProductionService.assertDesignRevisionAllowed()` dan logic increment `versiRevisi`. **Backend Finance**: Endpoint detail invoice, invoice PDF, dan daftar invoice/payment ditambahkan `@AllowCustomer()` + `@GetUser()` untuk validasi ownership (mencegah IDOR). **Backend Customer**: `createReview()` dilengkapi status gate (status order `DIKIRIM`), ownership check, dan pencegahan review ganda. **Frontend web**: Halaman `/pesanan` (list order dengan search) + `/pesanan/[id]` (detail order, timeline visual, shipment info, upload revisi desain, download invoice, submit review) + `/pesan?draft=id` (komponen `RepeatOrderReview` untuk mengedit kuantitas, mengganti jenis produk, menyimpan draft, dan checkout ulang). **Tests**: 152/152 tests passed, build 10/10 sukses. | Kontrol akses kepemilikan data (order, invoice, payment) diimplementasikan ketat menggunakan `@AllowCustomer` dan pengecekan di level service. Gerbang status (cutting untuk revisi desain, dikirim untuk review) ditegakkan di backend untuk integritas data. Duplikasi order melakukan lookup harga dasar server-side dari tabel `ProductPriceList`. | CI HIJAU TERVERIFIKASI: run [29636580744](https://github.com/mlvweardev-source/mlv/actions/runs/29636580744) commit ddfb8bc - completed success. Menunggu konfirmasi siap lanjut ke **Fase 10 Bagian 4: Customer Chat**. |
| 10.4 | 2026-07-18 | **Fase 10 Bagian 4 selesai: Customer Chat (penutup Fase 10)**. **Database (�6.8)**: model `CustomerChatThread` (id, order_id unique, customer_id FK RESTRICT) + `CustomerChatMessage` (id, thread_id, sender_type enum, sender_id nullable, pesan, created_at) + enum `CustomerChatSenderType` (`customer`/`admin`/`ai_bot` � `ai_bot` schema-only untuk Fase 12, logic belum ada). Migrasi `20260718031255_add_customer_chat` � **FIX enum case**: SQL awal pakai snake_case `customer_chat_sender_type` (outlier, konvensi codebase PascalCase); rename type di DB + edit migrasi jadi `"CustomerChatSenderType"` + `prisma migrate resolve --rolled-back` + re-deploy. Relasi `Order.customerChatThread` + `Customer.customerChatThreads` ditambahkan ke schema. **Backend** (`services/api/src/common/customer-chat/`): pola SSE **identik** dengan Internal Chat Fase 9.4 � `@Sse()` + `Subject msg$` + `interval(25s)` ping (SSE comment `':'`) + `merge()` + subscriber Map in-memory per proses; **tidak ada mekanisme realtime baru**. `CustomerChatService.validateAccess()` defense-in-depth: Penjahit ditolak ForbiddenException tanpa DB lookup, lalu ownership check `actor.sub === order.customerId` untuk pelanggan (pola `OrderService.getOrderById`). `getOrCreateThread` upsert dengan `customerId` dari order (sumber kebenaran, bukan token). `sendMessage` menurunkan `senderType` dari actor (`CUSTOMER?'customer'`, `USER?'admin'`); nama pengirim di-resolve **batch** (`prisma.customer.findMany` + `authService.getUsersByIdsInternal`) � tidak N+1. Controller `@Roles(OWNER, MANAJER_PRODUKSI)` + `@AllowCustomer()` class-level. **Endpoint �8**: `GET/POST /orders/:id/customer-chat` + `GET .../stream` (SSE). **UI pelanggan** (`apps/web`): `components/chat-panel.tsx` (CustomerChatPanel) ditanam di `/pesanan/[id]` sebagai section full-width di bawah grid; bubble kanan=customer (semua `senderType='customer'` adalah "saya" � pelanggan satu-satunya customer di threadnya), kiri=admin dengan nama. **UI staf** (`apps/admin`): `components/customer-chat-panel.tsx` ditanam di `/orders/[id]` sebagai Card terpisah di samping Internal Chat; bubble kanan=admin-untuk-userId-saya, kiri=customer (prefik "Pelanggan � nama")/admin lain/AI (Fase 12); **Card dibungkus `{canAct && ...}` � disembunyikan untuk Penjahit** (�5.1: Customer Chat bukan fokus Production). **Demo `pnpm --filter @mlv/api demo:customer-chat` 20/20 LULUS**: (1) customer A GET thread sendiri 200 + customerId terikat; (2) customer A POST pesan `senderType='customer'` + senderId benar; (3) Owner GET thread customer 200 + nama customer "Andi Chat" ter-resolve; (4) Owner POST balasan `senderType='admin'`; (5) customer A GET ulang lihat balasan admin; (6) **RBAC ownership**: customer B GET/POST thread A ? 403, tidak ada pesan B masuk DB (count tetap 2); (7) **RBAC role**: Penjahit GET/POST ? 401 (AuthGuard `@Roles` mismatch � konvensi existing codebase, 401 bukan 403 untuk role salah); (8) **SSE end-to-end**: stream dibaca via `fetch` + `getReader()`, POST pesan paralel dari Owner, pesan `Pesan realtime via SSE demo!` sampai di stream dengan `senderType='admin'` terverifikasi. **Tests**: 159/159 pass (16 suites, +7 baru: 5 RBAC validateAccess + 2 senderType derivation). Build 10/10, turbo lint 13/13, turbo test 6/6 hijau. Commit `7df1f86`. | **Pola reuse SSE**: Customer Chat pakai persis pattern Internal Chat Fase 9.4 � service di `src/common/` (BUKAN domain bounded context, melintasi Order+Identity+Customer); subscriber Map in-memory per proses (cukup untuk modular monolith 1 proses, kalau multi-instance perlu Redis pub/sub � documented as known minor debt sama seperti Internal Chat). **AI bot schema-only**: enum `ai_bot` di-resolve nama default `"AI Assistant"` di `defaultSenderName()` tapi TIDAK ada logic auto-reply � itu Fase 12 (AI Customer Support). **SenderId nullable**: migration set `sender_id TEXT` (bukan NOT NULL) karena `ai_bot` mungkin tidak punya user id; untuk fase ini `senderId` selalu diisi (customer atau staff id). **AuthGuard 401 vs 403**: role mismatch melempar `UnauthorizedException` (401) di AuthGuard level � konvensi existing codebase sejak Fase 1; service-level `validateAccess` melempar `ForbiddenException` (403) untuk ownership violation. Demo menerima 401/403 untuk Penjahit (keduanya = ditolak). **CustomerId dari order**: `getOrCreateThread` selalu lookup `order.customerId` dulu (bukan dari token pelanggan) supaya thread yang dibuka staf tetap terikat ke customer pemilik order � token staf tidak punya customerId. **UI bubble customer**: dari sisi pelanggan, semua `senderType='customer'` = bubble kanan ("saya") karena pelanggan adalah satu-satunya customer yang bisa posting di thread miliknya (ownership check memastikan ini); tidak perlu `userId` client-side, lebih sederhana daripada pola staff. **Minor debt**: `unsubscribe()` di SSE controller di-`void` (paritas Internal Chat � NestJS @Sse tidak beri destroy signal default, leak kecil per koneksi, acceptable untuk portal dengan user terbatas). | **CI HIJAU TERVERIFIKASI**: run [29639722423](https://github.com/mlvweardev-source/mlv/actions/runs/29639722423) commit 7df1f86 � Status **SUCCESS**, 1m 53s, 11 warnings (semua pre-existing `any`/unused dari Fase 10.2/10.3 + Internal Chat panel Fase 9.4, BUKAN dari file baru Customer Chat yang bersih). **PENUTUP FASE 10 SEPENUHNYA** � 4 bagian selesai: (10.1) Auth pelanggan OTP WA + Google OAuth asli + portal shell apps/web; (10.2) Checkout flow aman dari manipulasi harga + status gates; (10.3) Portal existing-customer (riwayat pesanan, repeat order, review, invoice download); (10.4) Customer Chat realtime SSE. Total tests 152?159, total CI runs Fase 10: 45 (run #36?#45). **Menunggu keputusan user sebelum lanjut ke Fase 11: Integrasi Payment Gateway lanjutan (job otomatis pelepasan reservasi kadaluarsa � ditunda sejak Fase 5). Jangan mulai Fase 11 sebelum user bilang "lanjut".** |

## Ringkasan Penutup Fase 10 � Portal Pelanggan Lengkap (2026-07-18)

Fase 10 membangun **seluruh permukaan pelanggan publik** dari nol � sebelumnya hanya staf yang punya portal (`apps/admin`). Sekarang pelanggan punya `apps/web` sendiri dengan 4 bagian yang saling melengkapi:

| Bagian | Fokus | Output Utama |
|--------|-------|--------------|
| 10.1 | Auth + shell | OTP WA via Fonnte (queue), Google OAuth asli (google-auth-library), cookie `mlv_customer_token` httpOnly terpisah, proxy.ts proteksi `/pesanan`+`/akun`, landing+login |
| 10.2 | Checkout baru | Cek ketersediaan BOM real-time, anti-manipulasi harga (server-side `ProductPriceList`), auto-DP 50%, gate status DP/Pelunasan, redirect Midtrans Snap |
| 10.3 | Existing customer | Riwayat pesanan + detail (timeline, shipment, revisi desain, invoice PDF download, review), repeat order dengan edit kuantitas |
| 10.4 | Customer chat | Chat pelanggan?admin realtime SSE (pola sama Internal Chat), RBAC ownership customer + role Penjahit ditolak, UI di kedua portal |

**Keputusan teknis penting Fase 10**: (1) Google OAuth `sub` sebagai identifier (bukan email, link-by-email hanya jika verified); (2) JWT pelanggan stateless 7 hari TANPA refresh token (beda dari staff yang punya refresh); (3) cookie `mlv_customer_token` terpisah `mlv_access_token` (dev share host localhost); (4) harga dasar server-side dari `ProductPriceList` menutup celah manipulasi harga client; (5) SSE per subscriber Map in-memory (cukup untuk 1 proses modular monolith); (6) Customer Chat = tabel & service terpisah dari Internal Chat meski pola SSE sama.

**Hal yang perlu diputuskan sebelum Fase 11** (job otomatis pelepasan reservasi kadaluarsa):
1. **TTL reservasi** � berapa lama reservasi stok DP bertahan sebelum auto-release? (saran: 24 jam sejak checkout, sesuai praktik umum e-commerce � DP tidak dibayar dalam 1 hari = stok dilepas & order bisa dibatalkan/ditandai expired). Alternatif: 1 jam (agresif), 7 hari (longgar).
2. **Aksi pasca-release** � setelah reservasi dilepas otomatis, order-nya bagaimana? (a) Auto-`DIBATALKAN`; (b) tetap `MENUNGGU_PEMBAYARAN_DP` tapi stok bebas dipakai order lain (customer masih bisa bayar DP, jika stok masih ada saat bayar ? re-reserve, jika habis ? tolak); (c) pindah ke status baru `KEDALUWARSA` (perlu tambahan enum). Rekomendasi: (a) auto-batalkan + notifikasi WA "Pesanan dibatalkan karena DP tidak dibayar dalam 24 jam, silakan pesan ulang".
3. **Scheduler mechanism** � BullMQ repeat job (sudah ada infra Fase 6) vs `setInterval` di API process vs cron eksternal? Rekomendasi: BullMQ repeat job (`Queue.upsertJobScheduler` atau `repeat.cron`) � sudah ada Redis+worker, idempotency via DB state check (pola Fase 6).
4. **Midtrans expiry** � Midtrans Snap punya expiry sendiri (default 24 jam, bisa di-set di Snap request). Apakah TTL reservasi sinkron dengan Midtrans expiry, atau independen? Rekomendasi: set Midtrans `expiry.duration` saat buat payment = sama dengan TTL reservasi, supaya keduanya kadaluarsa bersamaan (UX konsisten).
5. **Payment Link expired** � bagaimana jika pelanggan klik link Midtrans yang sudah expired? Midtrans akan tampilkan halaman expired sendiri; di sisi kita, webhook `expire` dari Midtrans bisa trigger release reservasi (sudah ada webhook handler Fase 5, tinggal tambah handling `transaction_status: 'expire'`).

Item tertunda lain dari Fase 5 yang mungkin mau di-bundle ke Fase 11: refund workflow nyata (approval `REFUND` sudah ada sejak Fase 5 tapi effect-nya masih placeholder), MidtransSnap `enabled_payments` filtering, payment gateway alternatif (transfer manual VA).

| 11 | 2026-07-18 | **Fase 11 selesai: Integrasi Payment Gateway Lanjutan � Reservasi Kadaluarsa & Refund**. **TTL reservasi 24 jam**: default di `inventory.service.ts` diubah dari 15 menit ke 24 jam; `executeCheckout` mengirim `expiresAt` eksplisit 24 jam. **Midtrans Snap expiry**: `initMidtransSnap` menambah parameter `expiry.duration: 24, unit: 'hour'` � Snap link dan reservasi kadaluarsa bersamaan. **Scheduler reservasi kadaluarsa**: `ReservationExpiryProcessor` (BullMQ repeatable job, interval 15 menit via `Queue.upsertJobScheduler`) � query `stock_reservations` ACTIVE + `expires_at` terlewati ? per order: release reservasi + cancel order + publish `ReservationExpired` event + activity log. Module `ReservationExpiryModule` di-register di `AppModule`. **Webhook expire handling**: `PaymentExpired` dirutekan ke `ORDER_EVENTS` + `INVENTORY_EVENTS` + `NOTIFICATION_EVENTS`; `OrderEventsProcessor.handlePaymentExpired()` cancel order (idempotent: skip jika bukan `MENUNGGU_PEMBAYARAN_DP`) + publish `ReservationExpired` untuk WA notification. Dua jalur (scheduler + webhook) idempoten via cek status reservasi ACTIVE + status order. **Event baru**: `ReservationExpired` (EVENT_NAMES + routing ? NOTIFICATION_EVENTS), payload `ReservationExpiredPayload extends CustomerContactFields` (orderNumber, customerNama, customerNoHp). **Notification template**: `reservation.expired` ? WHATSAPP: "pesanan dibatalkan otomatis karena DP belum dibayar dalam 24 jam". **Refund Midtrans sungguhan**: `executeApprovalEffect` REFUND case � sebelum efek internal, panggil Midtrans `POST /v2/{order_id}/refund`; gagal ? `BadRequestException` (efek internal TIDAK jalan, approval tidak dieksekusi); sukses ? payment status ? SUCCESS lalu release + cancel. `callMidtransRefund()` mencari payment SUCCESS, bikin refund_id unik, POST ke sandbox/production API. Payment status dicatat (SUCCESS/FAILED) sebagai jejak staf. **PaymentExpired event diperkaya** dengan customer contact (nama, noHp, orderNumber) untuk render template WA. **Tests**: 18 suites, 172/172 pass � termasuk test baru `reservation-expiry.processor.spec.ts` (5 tests: normal, skip non-MENUNGGU_PEMBAYARAN_DP, multiple orders) + `finance.refund.spec.ts` (4 tests: refund sukses, API gagal, no payment, payment FAILED) + test `handlePaymentExpired` di `order.service.spec.ts` (4 tests). **CI hijau terverifikasi** (run [29645856748](https://github.com/mlvweardev-source/mlv/actions/runs/29645856748), commit `d3d0635`): Build + Migrate + Lint + Test semua success. | Keputusan user dieksekusi: (1) TTL 24 jam; (2) auto-DIBATALKAN + WA; (3) BullMQ repeat job; (4) Midtrans expiry disamakan; (5) webhook expire trigger release+cancel. Item tertunda: `enabled_payments` filtering, VA transfer manual (bukan scope fase ini). | Menunggu konfirmasi siap lanjut ke **Fase 12: AI Domain**. |
| 12.1 | 2026-07-18 | **Fase 12 Bagian 1 selesai: Fondasi AI Gateway & Design Analyzer**. **packages/ai**: GeminiClient wrapper (@google/generative-ai, model gemini-2.0-flash); RateLimiter (Redis-based, 50 req/jam per pelanggan, key 
atelimit:{service}:{customerId}); prompt templates (design-analyzer.ts ? system prompt + user prompt, terpisah dari logic). **services/ai-gateway**: DesignAnalyzerModule (controller + service); RateLimiterMiddleware (reusable, Redis-backed, fail-open jika Redis down); POST /ai/design-analyzer endpoint (terima catatanTeks + productType, return JSON terstruktur: warna, lokasi_print, estimasi_kompleksitas, saran); ConfigModule untuk GEMINI_API_KEY; CORS enabled. **services/api (Order Domain)**: createDesignRecord panggil ai-gateway secara **synchronous** (10 detik timeout) ? hasil AI langsung tersimpan di order_designs.hasil_ekstraksi_ai; PATCH /orders/:id/designs/:designId/confirm endpoint untuk konfirmasi/tolak hasil AI (DITERIMA/DITOLAK); **fallback-safe**: AI failure TIDAK memblokir design upload (hasil 
ull). **Frontend (apps/web)**: /pesan ? kartu hasil AI muncul setelah upload desain (warna, lokasi print, kompleksitas, saran); /pesanan/[id] ? detail desain menampilkan hasil AI + tombol Terima/Tolak; OrderDesign type ditambah hasilEkstraksiAi. **Env baru**: AI_GATEWAY_URL di .env.example (default http://localhost:3002). **Tests**: 19 suites, 189/189 pass (packages/ai: 10, ai-gateway: 7, api: 172). **CI hijau** (run [29655636334](https://github.com/mlvweardev-source/mlv/actions/runs/29655636334) commit 8319f57). | AI selalu asistif (?17.4, ?17.5): timeout 10 detik, fallback 
ull, tidak pernah blocking. Rate limiter reusable untuk layanan AI berikutnya. Struktur per layanan AI (?9): siap untuk Quotation Assistant, Customer Support, dst. | Menunggu konfirmasi lanjut **Fase 12 Bagian 2** (Quotation Assistant & Customer Support). |

| 12.2 | 2026-07-19 | **Fase 12 Bagian 2 selesai: Quotation Assistant & Customer Support** � 2 layanan AI baru dengan **level kepercayaan berbeda** sesuai �17.4 vs �9. **Quotation Assistant (�17.4) � WAJIB konfirmasi manusia**: `packages/ai` prompt template (quotation-assistant.ts � system prompt tegas "HANYA memberi saran, harga final manusia lewat approval"); `services/ai-gateway` QuotationAssistantModule + `POST /ai/quotation-assistant` (rate limited shared 50/jam) � return range harga per pcs + total estimasi + alasan + faktor_pendorong_harga; `services/api` AiAssistantService (HTTP proxy ke ai-gateway, fail-safe timeout 15s, **return null kalau gagal � caller handle sbg "AI tidak tersedia"**) + AiAssistantController `POST /ai-assistant/quotation` (@Roles OWNER+MANAJER_PRODUKSI); `apps/admin` QuotationAssistantDialog di halaman Order detail ("Minta Saran Harga AI" button) � Owner/Manajer bisa pilih item + tambah catatan, hasil AI ditampilkan sbg saran (bukan final). **Verifikasi �17.4**: demo cek basePriceSnapshot order_item TIDAK berubah setelah AI saran & order status TETAP DRAFT � AI tidak pernah auto-apply. Hasil AI jadi input untuk **flow Approval "Harga Khusus" yang sudah ada** (Fase 5/9.3) � staf tetap input harga final. **Customer Support (�9) � BOLEH auto-reply dari konteks, WAJIB eskalasi kalau di luar**: `packages/ai` prompt template (customer-support.ts � system prompt tegas "HANYA jawab dari konteks order, TIDAK menebak" + daftar eksplisit pertanyaan yang HARUS eskalasi: diskon, komplain, revisi); `services/ai-gateway` CustomerSupportModule + `POST /ai/customer-support` (rate limited shared 50/jam) � return `{ canAnswer, jawaban, alasan_eskalasi }`; `services/api` AiAssistantController customer-support endpoint (@AllowCustomer � konteks dikirim lengkap di payload, **ai-gateway TIDAK query balik** (ai-gateway proses terpisah, paralel dengan prinsip Fase 8 untuk Notification � lihat klarifikasi eksplisit di entry koreksi bawah)). **CustomerChatService integrasi**: setelah customer kirim pesan, panggil AI di **background** (tidak awaited � response POST /customer-chat tidak terblokir); kalau canAnswer=true ? post balasan `senderType='ai_bot', senderId=null` + push SSE (paritas dengan alur customer/admin); kalau canAnswer=false ? TIDAK post apa-apa, pesan pelanggan tetap masuk thread, staf balas manual seperti biasa. Konteks order lengkap (status, items, timeline, payments, invoices, shipment) dikumpulkan via Prisma langsung di CustomerChatService � **TIDAK query domain lain**, paritas dengan Notification Fase 8. **Verifikasi �9**: demo cek 3 pertanyaan out-of-context (diskon, komplain, revisi spesifikasi) ? AI TIDAK jawab (canAnswer=false), thread bertambah tapi tanpa balasan AI; 2 pertanyaan in-context (status, ai_bot; thread akhir berisi campuran customer payment) ? AI jawab otomatis sbg / ai_bot / admin. **Shared improvements**: GeminiClient tambah `jsonMode` option (response_mime_type='application/json' � Gemini structured output mode) + lebih robust JSON parsing dengan regex fallback extraction; dipakai Quotation & Customer Support. **Tests (monorepo-wide, 4 package)**: 32 suites, 246 tests pass (packages/ai: 6/36, ai-gateway: 6/28, services/api: 19/177, notification: 1/5). [Catatan: angka '22 suites, 241 tests' di laporan awal fase ini tidak akurat � adalah jumlah 3 package tanpa notification, suites-nya sendiri salah hitung. Direvisi pakai scope monorepo penuh.] **Demo scripts**: `pnpm --filter @mlv/api demo:quotation-assistant` (15 checks: Owner/Manajer dapat saran, Penjahit ditolak, Customer ditolak, qty 0 ditolak, **basePriceSnapshot TIDAK berubah**) + `pnpm --filter @mlv/api demo:customer-support` (10 checks: in-context auto-reply, out-of-context eskalasi, owner balas manual, thread berisi campuran customer/ai_bot/admin). **Real API manual test**: 2/3 Customer Support real tests passed dengan response valid (`canAnswer=true` pertanyaan "Kapan selesai?" ? jawaban valid, `canAnswer=false` pertanyaan "Bisa minta diskon 30%?" ? alasan_eskalasi="Permintaan diskon merupakan kebijakan staf MLV Konveksi dan tidak dapat kami jaw..."). Quota free tier Gemini 2.5 Flash (20/hari) tercapai setelah 2 tests � Quotation real test dijadwalkan ulang besok atau via CI dengan secret terpasang. Setelah identifikasi JSON parse issue (response mengandung teks di luar JSON object), diperbaiki dengan `jsonMode: true` (response_mime_type='application/json') + regex fallback extraction di kedua service. **CI hijau terverifikasi** (run [29672464290](https://github.com/mlvweardev-source/mlv/actions/runs/29672464290) commit fa4b7b9): 0 errors, 11 warnings (semua pre-existing � Fase 5 technical debt any di catch blocks). | **Prinsip kerja dibedakan per layanan (�17.4 vs �9)**: Quotation = saran saja, tidak pernah auto-ubah harga � Approval "Harga Khusus" yang sudah ada (Fase 5) jadi gerbang manusia. Customer Support = auto-reply AMAN untuk pertanyaan dari konteks order aktual, tapi TIDAK PERNAH menjawab di luar konteks � eskalasi ke manusia. **Rate limiter shared** (50/jam/pelanggan) untuk semua layanan AI � paritas keputusan Bagian 1. **Prinsip Fase 8 dijaga**: ai-gateway TIDAK query balik domain lain, semua konteks ditaruh lengkap di payload. **Struktur per-layanan AI (�9)**: 3 modul sekarang jalan paralel (DesignAnalyzer, QuotationAssistant, CustomerSupport) � semua rate-limited via middleware reusable. | Konfirmasi siap lanjut ke **Fase 12 Bagian 3** (Production Assistant & Inventory Prediction). |

**Hal yang perlu diputuskan sebelum Fase 12 Bagian 2**:
1. **Quotation Assistant** ? apakah perlu menghitung harga otomatis berdasarkan desain yang dianalisis AI, atau cukup estimasi kasar berdasarkan kompleksitas?
2. **Customer Support chatbot** ? integrasi dengan customer chat yang sudah ada (Fase 10.4), atau chatbot terpisah?
3. **Monitoring** ? apakah perlu dashboard untuk memantau penggunaan AI (request count, token usage, error rate)?

**Jangan mulai bagian berikutnya sebelum aku bilang lanjut.**


## Koreksi Review Fase 12 Bagian 1 (2026-07-19)

**3 masalah ditemukan saat review dan sudah diperbaiki:**

1. **gemini-2.0-flash sudah di-shutdown Google (1 Juni 2026)** ? diganti **gemini-2.5-flash** (bukan 3.5-flash). Alasan pemilihan: (a) status stable/production, bukan preview � setelah insiden 2.0-flash di-shutdown mendadak, prioritas model tidak rawan perubahan tiba-tiba; (b) skor FACTS Grounding lebih tinggi � penting untuk Customer Support bot dan Sales Insight yang jawabannya harus akurat terhadap data bisnis nyata; (c) biaya lebih rendah ($0.30/$2.50 per 1M token vs $1.50/$9 untuk 3.5-flash). Real API test ditambahkan (gemini-client-real.spec.ts) � skip jika GEMINI_API_KEY tidak set, tapi saat ada key, benar-benar hit API dan verifikasi response JSON valid.

2. **RateLimiterMiddleware fail-open ? fail-closed**. Alasan: rate limiter = kontrol biaya; saat Redis down (kita tidak bisa pantau usage), sistem seharusnya TIDAK membuka lebar. Karena AI memang asistif (boleh tidak jalan), fail-closed menghasilkan experience yang sama seperti AI gagal karena sebab lain ? upload desain tetap jalan.

3. **Demo scripts ditambahkan**: demo-ai-fallback.ts (upload saat AI gateway mati) + demo-ai-rate-limit.ts (51st request ditolak 429). Kedua demo butuh Docker Compose (DB + Redis) untuk dijalankan lokal.

### Verifikasi Real (2026-07-19)

**Real API Test (gemini-2.5-flash)**:
- ? API call berhasil, usage metadata diterima (prompt=9, candidates=1, total=15)
- ? Design Analyzer response valid: complexity=RENDAH, print locations=1, warna kain=biru Navy
- ? JSON structure sesuai skema (warna, lokasi_print, estimasi_kompleksitas)

**Demo Fallback**:
- ? AI gateway down ? design tetap tersimpan dengan `hasilEkstraksiAi=null`
- ? Order tidak rusak, masih bisa lanjut ke checkout
- ? 0 failure(s)

**Demo Rate Limit**:
- ? 50 request pertama: diterima (200)
- ? Request ke-51: ditolak 429 dengan pesan "Batas permintaan AI tercapai (50 per jam)"
- ? Headers: X-RateLimit-Limit=50, X-RateLimit-Remaining=0, X-RateLimit-Reset=3565
- ? 0 failure(s)

**CI hijau setelah perbaikan**: run [29668885758](https://github.com/mlvweardev-source/mlv/actions/runs/29668885758) commit 2e06201. 19 suites, 192/192 pass (packages/ai: 13, ai-gateway: 7, api: 172).

## Koreksi Review Fase 12 Bagian 2 (2026-07-19) � DDD boundary + klarifikasi cross-PROCESS vs cross-DOMAIN

**2 masalah koreksi ditegakkan sebelum lanjut ke Bagian 3:**

### 1. CustomerChatService query lintas domain via Prisma langsung � MELANGGAR DDD �4.1

CustomerChatService.buildAiSupportContext() sebelumnya query `prisma.order.findUnique` (dengan include payments/invoices/shipments) � ini query lintas domain dalam SATU proses lewat Prisma langsung, bukan service method. FinanceService dan ShippingService TIDAK berada di proses terpisah seperti Notification � mereka di `services/api` yang sama, jadi aturan yang berlaku adalah **DDD �4.1 (cross-DOMAIN)**, BUKAN aturan Fase 8 (cross-PROCESS).

**Perbaikan**: ditambah 4 method `get*ForOrder` di service domain (pola sama dengan `getOrderByIdInternal` & `getOrderIdsForAssignee` yang sudah ada sejak Fase 3/4/5):
- `OrderService.getOrderContextForAi(orderId)` ? items + timeline (untuk AI)
- `OrderService.getOrderByIdInternal(orderId)` ? existing, dipakai untuk ownership check
- `FinanceService.getPaymentsForOrder(orderId)` ? payments
- `FinanceService.getInvoicesForOrder(orderId)` ? invoices
- `ShippingService.getShipmentForOrder(orderId)` ? shipment (return null kalau belum ada)
- `CustomerService.getCustomersByIdsInternal(ids[])` ? batch resolve nama customer (pola sama `getUsersByIdsInternal`)

CustomerChatService sekarang:
- `validateAccess()` ? `orderService.getOrderByIdInternal()`
- `getOrCreateThread()` ? `orderService.getOrderByIdInternal()` + `customerService.getCustomersByIdsInternal()`
- `sendMessage()` ? `orderService.getOrderByIdInternal()`
- `buildAiSupportContext()` ? `orderService.getOrderContextForAi()` + `financeService.getPaymentsForOrder()` + `financeService.getInvoicesForOrder()` + `shippingService.getShipmentForOrder()`

Pengecualian: tabel `customerChatThread` & `customerChatMessage` tetap diakses via Prisma langsung karena itu tabel milik CustomerChat domain sendiri (�6.8 cross-cutting chat infrastructure). `prisma.customer.findMany` dihapus � diganti `customerService.getCustomersByIdsInternal()`.

**Verifikasi**: `grep "prisma\.\(order\|payment\|invoice\|shipment\)\."` di `customer-chat.service.ts` = **0 hasil** (sebelumnya: 4 hits). CustomerChatModule imports `FinanceModule` & `ShippingModule` untuk inject service. Tests: `customer-chat.service.spec.ts` mock untuk `prisma.order.findUnique` dihapus; tambah mock untuk `OrderService.getOrderByIdInternal` (validateAccess, getOrCreateThread, sendMessage) + `FinanceService`/`ShippingService`/`CustomerService.getCustomersByIdsInternal`.

### 2. Klaim "paritas Fase 8" di entry 12.2 � KELIRU, perlu klarifikasi

Klaim "ai-gateway TIDAK query balik, paritas prinsip Fase 8 bahwa event payload harus lengkap di publisher" menyesatkan. Fase 8 berbicara tentang **lintas PROSES** (Notification di proses terpisah via BullMQ + Redis). Yang terjadi di CustomerChatService adalah **lintas DOMAIN dalam SATU proses** � aturan yang berbeda.

**Aturan yang benar (direvisi, jangan rujuk klaim 12.2 lama):**

| Skenario | Aturan | Mekanisme |
|----------|--------|-----------|
| **Lintas PROSES** (Notification, dll � proses terpisah via BullMQ/Redis) | Payload event harus LENGKAP di sisi publisher (Fase 8). Subscriber TIDAK query balik ke domain publisher. | EventBusService.publish() dengan payload komplet |
| **Lintas DOMAIN dalam SATU proses** (Order?Inventory, CustomerChat?Order/Finance/Shipping � semua di services/api) | Pakai SERVICE METHOD, BUKAN Prisma langsung (DDD �4.1). | Inject service lain, panggil `domainService.getXForY()` |

**Aplikasi konkret di Fase 12:**
- `ai-gateway` (proses terpisah) � payload AI context lengkap di sisi publisher (`services/api` kumpulkan via service method, kirim ke `ai-gateway` via HTTP). ai-gateway TIDAK query balik ke services/api. (Ini memang paralel dengan prinsip Fase 8, TAPI bukan "paritas" � ini karena ai-gateway memang proses terpisah, sama logikanya dengan Notification.)
- `CustomerChatService` (di services/api, satu proses dengan Order/Finance/Shipping) � panggil service method masing-masing domain. TIDAK query Prisma langsung ke tabel domain lain.

**Ringkasan untuk fase berikutnya**:
- Mau panggil data dari domain lain? Cek dulu: satu proses atau proses terpisah?
- Satu proses ? inject service, panggil method, BUKAN prisma langsung.
- Proses terpisah ? lengkapi payload di publisher (Fase 8).
- Jangan klaim "paritas Fase 8" untuk cross-DOMAIN � itu aturan berbeda, walaupun kelihatannya mirip.

**Tests setelah koreksi (monorepo-wide)**: 33 suites, 259 tests pass
- packages/ai: 6 suites, 36 tests
- services/ai-gateway: 6 suites, 28 tests
- services/api: 20 suites, 190 tests
- services/notification: 1 suite, 5 tests (1 todo, 4 passed)

**Diff vs sebelum koreksi (commit c0b45b2 Bagian 2)**: `+1 suite, +13 tests` � semua karena method internal baru: 4 (`finance.internal.spec.ts` file baru) + 3 (`customer.service.spec.ts` getCustomersByIdsInternal) + 3 (`shipping.service.spec.ts` getShipmentForOrder) + 3 (`order.service.spec.ts` getOrderContextForAi).

**Koreksi laporan saya sebelumnya**: angka "20 suites, 190/190 pass" di pesan koreksi hanya scope `services/api` saja, bukan monorepo. Angka "22 suites, 241 tests" di entry 12.2 juga tidak akurat � actual saat itu 30 suites (3 package tanpa notification) atau 32 suites (monorepo penuh). Tidak ada test hilang selama koreksi; framing-nya yang inconsistent. Mulai dari sini, laporan test count pakai scope monorepo-wide (4 package) supaya konsisten.

**CI hijau setelah koreksi**: run [29673417845](https://github.com/mlvweardev-source/mlv/actions/runs/29673417845) commit d63cdee � 3m 41s, 0 errors, 11 warnings (semua pre-existing Fase 5 technical debt).


| 12.3 | 2026-07-19 | **Fase 12 Bagian 3 selesai: Production Assistant & Inventory Prediction** � penutup Fase 12 (Sales Insight ditunda). **Production Assistant (�9)**: packages/ai prompt template (production-assistant.ts); services/ai-gateway ProductionAssistantModule + POST /ai/production-assistant (rate limited); services/api ProductionService.getProductionContextForAi() + AiAssistantService.suggestProductionAnalysis() + AiAssistantController POST /ai-assistant/production-assistant (@Roles OWNER+MANAJER); apps/admin kanban panel � dropdown pilih order + tombol "Analisis Produksi" ? insight panel (bottleneck detection, saran prioritas, estimasi lead time). **Inventory Prediction (�9)**: packages/ai prompt template (inventory-prediction.ts); services/ai-gateway InventoryPredictionModule + POST /ai/inventory-prediction (rate limited); services/api InventoryService.getInventoryContextForAi() + OrderService.getOrderVolumeTrends() + AiAssistantService.predictInventory() + AiAssistantController POST /ai-assistant/inventory-prediction (@Roles OWNER+MANAJER); apps/admin tab "Prediksi AI" di halaman Inventory � tabel material dengan status (KRITIS/RENDAH/AMAN), estimasi habis, saran qty beli, tombol "Buat PO" yang prefill form PO (/inventory/purchases?materialId=X&qty=Y). **DDD boundary dijaga**: ProductionService.getProductionContextForAi() pakai orderService.getOrderByIdInternal() (bukan query prisma.order langsung); InventoryService.getInventoryContextForAi() hanya query tabel milik Inventory Domain (stock_balances, stock_movements, bill_of_materials, materials); AiAssistantController mengumpulkan data dari service method sebelum kirim ke ai-gateway. **Grep verifikasi**: prisma.(order|payment|invoice|shipment). di production.service.ts (new code) = 0; di inventory.service.ts = 0; di ai-assistant.controller.ts = 0. **Tests monorepo-wide**: 39 suites, 293 tests pass � packages/ai 8/48 (+2 suites, +12 tests), services/ai-gateway 10/45 (+4 suites, +17 tests), services/api 20/195 (+5 tests), services/notification 1/5. **Build** 10/10 sukses. **Lint** 0 errors, 187 warnings (semua pre-existing technical debt). **CI hijau terverifikasi**: run [29676151457](https://github.com/mlvweardev-source/mlv/actions/runs/29676151457) commit 212e645 � job ci success (2m 2s). | AI selalu asistif (�9): Production Assistant = rekomendasi bukan auto-reorder; Inventory Prediction = saran bukan auto-create PO. Rate limiter shared 50/jam. Prefill PO = staf tetap submit. Sales Insight ditunda sampai Fase 13+. | Menunggu konfirmasi lanjut **Fase 13: Dashboard Analytics & KPI**. |


## Ringkasan Penutup Fase 12 � AI Domain Lengkap (2026-07-19)

Fase 12 membangun **seluruh AI Domain** dari nol � 5 layanan AI yang masing-masing punya prompt template terpisah, endpoint terpisah, dan rate limiter shared 50/jam/pelanggan:

| Bagian | Fokus | Output Utama |
|--------|-------|--------------|
| 12.1 | Fondasi + Design Analyzer | packages/ai (GeminiClient, RateLimiter, prompt templates), services/ai-gateway skeleton, Design Analyzer (analisis desain ? struktur warna/kompleksitas), fallback-safe (AI gagal tidak blokir upload) |
| 12.2 | Quotation + Customer Support | Quotation Assistant (saran range harga, �17.4 wajib konfirmasi manusia), Customer Support bot (jawab dari konteks order, escalate kalau di luar), Koreksi DDD boundary (CustomerChatService pakai service method) |
| 12.3 | Production Assistant + Inventory Prediction | Production Assistant (deteksi bottleneck, saran urutan, estimasi lead time), Inventory Prediction (prediksi restock material, saran qty beli, prefill PO) |

**Keputusan teknis penting Fase 12**: (1) gemini-2.5-flash (bukan 2.0 yang sudah di-shutdown Google); (2) RateLimiter fail-closed (bukan fail-open) � kontrol biaya; (3) ai-gateway = proses terpisah ? payload harus lengkap di publisher (services/api kumpulkan via service method); (4) setiap layanan AI = module terpisah di ai-gateway + prompt template terpisah di packages/ai; (5) semua hasil AI = rekomendasi, tidak pernah auto-eksekusi.

**Fitur yang ditunda**: Sales Insight � butuh data Dashboard Analytics (Fase 13).

**Menunggu konfirmasi lanjut ke Fase 13: Dashboard Analytics & KPI.**

| 13 | 2026-07-19 | **Fase 13 selesai: Dashboard Analytics & KPI** — 12 metrik KPI + QC verification. **Prisma migration** (`20260719120000_add_qc_status_and_cost_fields`): `production_tasks` +4 field (`qc_status`, `qc_reason`, `qc_by`, `qc_at`); `production_routings` +1 field (`estimasi_biaya_jahit_per_pcs`, placeholder estimasi: Kaos=5000, Kemeja=8000, Hoodie=10000, Topi=3000, Tas=7000 Rp/pcs). **AnalyticsModule** (`services/api/src/domains/analytics/`): read-only aggregation, DDD boundary (0 Prisma access). `GET /analytics/dashboard?from=&to=` — satu endpoint, RBAC: Owner = full 12 metrik, Manajer Produksi = operasional saja. **14 internal analytics methods** di 7 domain services. **QC endpoint**: `PATCH /production/tasks/:id/qc` — Manajer Produksi/Owner verifikasi pass/reject setelah task SELESAI. **Dashboard UI** (`apps/admin`): KPI cards, line chart, pie chart, Top tables, metric cards, filter periode. **Formula**: Stock Accuracy = `1 - (COUNT(ADJUST) / COUNT(IN+OUT+ADJUST))` (placeholder); Response Time CS = avg(admin/ai_bot reply - customer msg), AI bot DIHITUNG. Profit = Omzet − Biaya Bahan − Estimasi Biaya Jahit (placeholder). **ESLint cleanup**: 11 warning Fase 9-12 → 0. **Tests**: 20 suites, 195/195 pass (services/api). **CI hijau**: run [29687954762](https://github.com/mlvweardev-source/mlv/actions/runs/29687954762) commit `1ce81f5` — status success, 2m 5s. | AnalyticsModule = read-only via service methods (DDD §4.1). RBAC di service layer. QC = verifikasi terpisah Manajer. Profit biaya jahit = placeholder. | — |
